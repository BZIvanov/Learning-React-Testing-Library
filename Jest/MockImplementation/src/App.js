import FollowIcon from './components/FollowIcon';

function App() {
  return (
    <div>
      <h1>My App file</h1>

      <p>
        In the test will provide custom content for the below FollowIcon
        component. This is helpful if we don't want to deal with the
        implementation of the below component.
      </p>
      <FollowIcon />
    </div>
  );
}

export default App;
